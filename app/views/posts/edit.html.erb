<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <%= form_for(@post, url: user_post_path(current_user, @post)) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      
      <%= render "fields", f: f %>
        
      <div class="form-group">
        <%= f.submit "UPDATE", class: "btn btn-sm btn-primary" %>
      </div>
    <% end %>
    
  </div>
</div>
